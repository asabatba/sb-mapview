{
  "name": "silverbullet-plug-gpxmap",
  "version": "0.1.0",
  "private": true,
  "description": "SilverBullet plug for rendering GPX tracks and waypoints on Leaflet maps",
  "scripts": {
    "build": "npx plug-compile gpxmap.yaml"
  },
  "dependencies": {
    "@silverbulletmd/silverbullet": ">=2.5.3"
  }
}
