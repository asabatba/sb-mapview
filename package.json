{
  "name": "silverbullet-plug-gpxmap",
  "version": "0.1.0",
  "private": true,
  "description": "SilverBullet plug for rendering GPX, GeoJSON, and manual marker maps with Leaflet",
  "scripts": {
    "build": "npx plug-compile gpxmap.yaml"
  },
  "dependencies": {
    "@silverbulletmd/silverbullet": ">=2.5.3"
  }
}
